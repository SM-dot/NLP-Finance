# Assignment 3: Measuring the Effect of Iran War Risk on Global Financial Markets

FRE-GY 7871 A · NLP and the Investment Process · Fall 2026

A replication of Rigobon and Sack, *The Effects of War Risk on U.S. Financial
Markets* (NBER 9609, 2003), applied to the 2026 Iran war, with the day-selection
step — the one step those authors did by hand — done with NLP instead: **flag each
trading day 1 (high war-news) or 0 (otherwise) from NLP, then let
heteroskedasticity-based identification do the estimating.** The pipeline also
implements and tests two concrete alternatives to that identification strategy
(a supervised/unsupervised day-classification comparison, and a plain event-study
regression run head-to-head against the headline method), a three-regime extension
of the two-regime framework, and a quantified check on the lexicon's vocabulary
coverage. See `REPORT.md` for the full write-up and `SUMMARY.md` for a
plain-language version; both have PDFs.

This folder is self-contained: run the scripts in order to rebuild everything from
scratch, then open `analysis.ipynb`. No data files are checked into git; `data/` is
gitignored and regenerated by the scripts, with one exception —
`data/news/verified_events.json` is tracked, since it is a manually-verified
research artifact (real news citations for Table 1), not something the automated
pipeline regenerates.

## The idea in one paragraph

War risk cannot be regressed on asset prices directly, because nobody observes it: it
is easy to say *when* war news broke, much harder to say by how much it moved the odds
of war. Rigobon and Sack's answer is to identify the effect from a shift in variance
rather than from the level of the factor. If you can name a set of days on which
war-related news was unusually intense, and assume every *other* driver of asset prices
was no more volatile than usual on those days, then the change in the covariance matrix
of asset returns between those days and ordinary days is driven entirely by the war
factor — and that is enough to recover each asset's sensitivity to it, without ever
measuring the factor. Identifying the day sets is therefore the whole ballgame, and it
is exactly the step NLP can do better than a person with a newspaper archive. This
update goes further and asks whether the *estimation* step itself — not just the
day-selection — is the best available choice, by building and testing the obvious
alternative (Section 6 of `REPORT.md`) rather than assuming the answer.

## Pipeline

| Script | What it does | Time |
|---|---|---|
| `scripts/00_test_estimator.py` | Monte Carlo check that the estimator recovers a known parameter and that OLS does not | ~20 s |
| `scripts/01_collect_news.py` | Five themed GDELT coverage timelines for the whole window (war, all-Iran, escalation, de-escalation, Hormuz) plus GDELT's tone series | ~2 min |
| `scripts/02_market_data.py` | Downloads eighteen financial variables from FRED and Yahoo Finance and builds the daily change matrix | ~1 min |
| `scripts/03_score_news.py` | Builds the daily war-risk news measures: attention, direction, contest | ~1 s |
| `scripts/04_build_event_days.py` | Selects the H/L day sets and the binary `war_news_flag`, writes Table 1 using verified real-news citations | ~5 s |
| `scripts/05_estimate.py` | Tables 2–6: the headline heteroskedasticity estimates, variance decomposition, rank condition, robustness checks | ~1 min |
| `scripts/06_figures.py` | Figures 1–3 | ~5 s |
| `scripts/07_regimes.py` | **New.** Three-regime split (bad/good/no war news), Tables 7–8, Figure 6 | ~10 s |
| `scripts/08_classify_days.py` | **New.** Supervised (logistic regression) and unsupervised (Gaussian mixture) day-classification alternatives, Tables 9–11, Figure 4 | ~15 s |
| `scripts/09_event_study.py` | **New.** The plain OLS event-study alternative, Table 12 | ~5 s |
| `scripts/10_novel_phrasing.py` | **New.** Vocabulary-coverage / "novel phrasing" diagnostic, Table 13 | ~1 s |
| `scripts/11_compare_methods.py` | **New.** Table 14 and Figure 5: sign/significance agreement across all methods | ~5 s |

```bash
pip install -r requirements.txt
cd scripts
python 00_test_estimator.py
python 01_collect_news.py      # re-run if it reports a series still missing
python 02_market_data.py
python 03_score_news.py
python 04_build_event_days.py
python 05_estimate.py
python 06_figures.py
python 07_regimes.py
python 08_classify_days.py
python 09_event_study.py
python 10_novel_phrasing.py
python 11_compare_methods.py
```

`01` talks to GDELT's *timeline* endpoint, which works reliably but still refuses an
occasional request; it caches each series to its own CSV and skips what it already
has, so re-running is safe. GDELT's *article* endpoint does not work reliably enough
within a session to source Table 1 — see "Why Table 1's sources were hand-verified"
below.

Then open `analysis.ipynb`, or build the PDFs directly:
`python build_report_pdf.py` (full technical report) and
`python build_summary_pdf.py` (plain-language version).

## Data and methods

**Sample window.** January 2 to September 11, 2026 — the last trading day for which
both the news and the market series are complete. That is 173 US trading days, of
which the first 38 precede the war: the US and Israel struck Iran on **February 28,
2026**.

**News data for day-selection.** GDELT DOC 2.0's *timeline* endpoint — five queries,
each returning the whole window in one request and counting every article GDELT
monitored. This is what actually selects the 18 high-news days; it is unaffected by
anything described below.

**Why Table 1's sources were hand-verified, not automated.** GDELT's *article*
endpoint (real news-wire content — article records carry their publishing domain,
e.g. reuters.com, aljazeera.com) refuses a large and essentially random share of
individual requests. This was tested at three request spacings (7s, 16s, 22s) with up
to 10 retry passes and 45-second cooldowns between passes, and none reliably cleared
whatever throttling this session's requests hit — a sustained run at 22-second spacing
produced zero successes across five minutes of continuous attempts. Wikipedia's
Current Events Portal doesn't throttle, but is a tertiary, community-edited source,
not journalism. **Table 1's 18 event descriptions were instead compiled through
targeted, one-day-at-a-time research
against Al Jazeera, CNN and Bloomberg reporting**, each with a source URL, stored in
`data/news/verified_events.json`. This is a one-time manual verification step that
runs *after* day-selection is complete — it does not affect which days are flagged or
any estimation result, only what Table 1 prints next to each date.

**The three NLP measures** (`scripts/03_score_news.py`), each standardised and
averaged into one war-news score: **attention** (coverage volume vs. its trailing
baseline), **direction** (escalation vs. de-escalation coverage share — used for the
three-regime split in Section 4 of the report, not for day-selection), and
**contest** (how split the coverage was, capturing "Unclear" days that carry real
information for a variance-based method even without a clear sign).

**Day sets.** Every trading day gets a binary `war_news_flag` (1 = high-news, top 18
of 173 by the news score; 0 = otherwise) — this literal 1/0 classification is what
feeds the heteroskedasticity estimator. A matched `set` column additionally marks
the L subset used for paired estimation (nearest trading day that is neither an H
day nor adjacent to one, and sits in the quiet half of the news-score distribution —
see "Three things worth flagging" below for why the quiet-half restriction is
necessary).

**Financial variables, estimator, and normalisation.** Eighteen variables across a
US, global, and sector block; each estimated against Brent (not the paper's
two-year yield, which does not respond to 2026's war risk strongly enough to serve
as the identifying variable — see `REPORT.md` Section 0/3); all three instrument
sets (ω₁, ω₂, ω₃), heteroskedasticity-robust standard errors.

## Additional analysis beyond the core replication

**Three regimes, not two** (`scripts/07_regimes.py`). War-news days are split into
bad-news (coverage skews escalatory), good-news (skews de-escalatory), and no-news.
A simple mean-comparison across these three groups matches the expected qualitative
pattern on **7 of 7** core variables (yields/oil/VIX up and equities down on
bad-news days; several reverse on good-news days) — see Table 7 / Figure 6. A
stricter, IV-based overidentification test (estimating the response separately from
bad-news and good-news sub-samples, following Rigobon 2003's multi-regime
framework) agrees in sign on 10/17 variables — an honest, noisier result given
sub-sample sizes of 4 and 8, reported alongside the cleaner descriptive finding
rather than in place of it.

**Two alternative day-classification methods** (`scripts/08_classify_days.py`): a
cross-validated logistic regression (features: the NLP measures; label: an
independent, market-based definition of a high-stress day) and an unsupervised
Gaussian mixture on the NLP features alone. The classifier's cross-validated ROC-AUC
is 0.43 — worse than random — which turns out to be informative rather than simply a
failure: the market-based label conflates all sources of volatility (including
non-Iran-related shocks), and the poor score is itself evidence for why
news-content-based day selection (not price-based selection) is the right design.
The core equity/credit results hold their sign across all three day-classification
schemes; VIX and the dollar are more sensitive to the choice.

**The event-study alternative, run head-to-head** (`scripts/09_event_study.py`). The
most standard alternative to heteroskedasticity — regress each variable's change on
the same 1/0 flag, plain OLS — finds **zero of eighteen** variables significant at
5%, against heteroskedasticity's 13 of 17 on the identical days. This is the
strongest piece of evidence for why the variance-based method, not the more
familiar mean-based one, is the right tool here: mixed-direction war news (8
escalatory, 4 de-escalatory, 6 unclear, among the 18 selected days) washes out in a
simple average but not in a variance comparison.

**A quantified vocabulary-coverage check** (`scripts/10_novel_phrasing.py`): 52.5%
of real, war-relevant sentences from the verified event text score zero hits
against the hand-built lexicon (facility names, invented compound nouns like "rogue
supertankers," and named agreements the lexicon could not have anticipated). This
affects sentence-level lexicon scoring, not the aggregate coverage-volume measure
the day-selection runs on.

**A literature review of further alternatives** (Geopolitical Risk Index, Economic
Policy Uncertainty Index, Markov-switching volatility regimes, structural VAR with
sign restrictions, direct LLM severity scoring) considered but not implemented, with
reasons — see `REPORT.md` Section 9.

## Three things worth flagging about the method (carried forward from v1)

**The equal-sized-sets requirement is load-bearing, not cosmetic.** The paper defines
its instrument over "an equal-sized set of other days". With unequal sets the
instrument's moment condition becomes n_H·E_H[Δx²] − n_L·E_L[Δx²] rather than the
difference in variances, and the estimator is badly biased rather than merely
noisier — in the Monte Carlo in `00_test_estimator.py`, an 18/159 split returns +0.9
when the truth is −3.75. `src/heteroskedasticity.py` weights each regime by 1/n so
the moment condition is right either way.

**The paper's normalising variable does not work in 2026.** Rigobon and Sack anchor
everything on the two-year Treasury yield, whose variance rose 6.2× on their
war-news days. In 2026 the two-year's variance is only 1.05× higher on war-news days.
Brent, whose variance rises 6.2×, takes its place; both normalisations are reported.

**The footnote 7 rule needs adapting to a running war.** The paper picks comparison
days "as close as possible to" the war-news days. Its window was the *run-up* to a
war, where nearby days really were quiet. 2026 contains six months of running war, so
the literal rule filled the comparison set with war days and briefly broke
identification. Comparison days are restricted to the quiet half of the news-score
distribution before picking the nearest one.

## Repository layout

```
assignment-3/
  README.md                 this file
  AI_USE.md                 AI use statement
  REPORT.md / REPORT.pdf    the full technical write-up
  SUMMARY.md / SUMMARY.pdf  plain-language companion
  analysis.ipynb            all exhibits, with output saved
  requirements.txt
  build_report_pdf.py / build_summary_pdf.py
  src/
    config.py               paths, window, queries, the variable list
    gdelt.py                throttled, retrying GDELT client
    warrisk_lexicon.py      the hand-built escalation/de-escalation lexicon
    news_score.py           FinBERT over text
    event_days.py           the war-news score and the H/L selection
    heteroskedasticity.py   the Rigobon-Sack estimators and diagnostics
    classify.py             new: supervised/unsupervised day-classification
  scripts/                  the eleven pipeline scripts above, run in order
  data/                     gitignored except data/news/verified_events.json
  figures/                  Figures 1-6
  report_tables/            Tables 1-14 (CSV)
```

## Sources

- Rigobon, R. and B. Sack (2003), "The Effects of War Risk on U.S. Financial
  Markets," NBER Working Paper 9609.
- Rigobon, R. (2003), "Identification through Heteroskedasticity," *Review of
  Economics and Statistics* 85(4), 777–792.
- Caldara, D. and M. Iacoviello (2018), "Measuring Geopolitical Risk," AER.
- Baker, S., N. Bloom and S. Davis (2016), "Measuring Economic Policy Uncertainty," QJE.
- Hamilton, J. (1989), "A New Approach to the Economic Analysis of Nonstationary
  Time Series and the Business Cycle," Econometrica.
- Uhlig, H. (2005), "What Are the Effects of Monetary Policy on Output?," JME.
- News data: GDELT Project DOC 2.0 API (day-selection); Al Jazeera, CNN and
  Bloomberg (Table 1 citations, verified by hand).
- Market data: FRED (Federal Reserve Bank of St. Louis) and Yahoo Finance.
